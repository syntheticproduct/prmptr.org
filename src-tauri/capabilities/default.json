{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions granted to the main editor window. This is the only window that needs the file-picker dialog plugin (open/save dialogs); the global-settings window is scoped separately in global-settings.json.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
