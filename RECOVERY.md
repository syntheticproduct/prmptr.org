# RECOVERY.md — prmptr.org

**Rebuild difficulty:** EASY
(Everything needed to build and run is committed and pushed. Credentials matter only
for publishing, not for getting the app working.)

## What this is
Tauri (Rust + Next.js + Milkdown) desktop app for prompt engineering — a markdown editor
plus a browser over local Claude Code / Claude Desktop session data. It owns no database:
it reads and writes files that already exist on the machine.

## Rebuild from scratch
1. `gh repo clone syntheticproduct/prmptr.org && cd prmptr.org`
2. Node 24.15.0 (`.nvmrc`; `.npmrc` sets `engine-strict=true`, so older Node is refused).
   Rust 1.95.0 installs itself from `rust-toolchain.toml` via rustup.
3. `sudo apt install libwebkit2gtk-4.1-dev libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev pkgconf`
4. Windows cross-build from WSL: `sudo apt install mingw-w64 nsis` and
   `rustup target add x86_64-pc-windows-gnu`.
5. `npm ci` — `package-lock.json` and `src-tauri/Cargo.lock` are both committed.
6. Credentials, only if publishing: `gh auth login` (releases), `npx wrangler login`
   (landing page). Optional `cp .env.example .env.local` holds dev conveniences, no secrets.
7. Verify: `npm run check && npm run test:unit && npm run test:rust`, then `npm run tauri dev`.
   Windows artifact: `npm run tauri build -- --target x86_64-pc-windows-gnu --bundles nsis`.

## Live data
The app is a viewer/editor over data it does not own, and creates no database.

| what | engine | lives at | size | irreplaceable? |
|---|---|---|---|---|
| Your markdown files | plain files | wherever the folder pane is pointed | n/a | yes — but yours, not the app's |
| Claude Desktop cowork sessions (read-only source) | JSON + LevelDB | `/mnt/c/Users/camil/AppData/Local/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Roaming/Claude/local-agent-mode-sessions` | 2.1 GB | yes, but owned by Claude Desktop |
| UI prefs (pins, pane width, last tab/view/root) | WebView localStorage | installed app's WebView2 profile | KB | no — regenerated by clicking |

**Back up:** `tar czf ~/cowork-sessions-backup.tgz -C "/mnt/c/Users/camil/AppData/Local/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Roaming/Claude" local-agent-mode-sessions`
**Restore:** `tar xzf ~/cowork-sessions-backup.tgz -C "/mnt/c/Users/camil/AppData/Local/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Roaming/Claude"`
**Current dump:** none exists. Losing it costs this project only its browsing targets.

Those transcripts may hold private/client material, so that tarball is as sensitive as the
conversations in it. This repo is PUBLIC on GitHub.

## What you need that isn't in the repo
| what | where it lives now | how to get it again |
|---|---|---|
| Roadmap / design handoff (the north-star spec `CLAUDE.md` points at) | `~/.claude/projects/-home-camille/memory/refs/prmptr-roadmap/` — 64 KB, in no git repo | nowhere; source `C:\Users\camil\Downloads\prmptr.org.zip` already deleted |
| GitHub token | `~/.config/gh/hosts.yml` (account `syntheticproduct`; gist/read:org/repo/user/workflow) | `gh auth login` |
| Cloudflare token for the landing site | `~/.config/.wrangler/config/default.toml` (oauth + refresh) | `npx wrangler login` |
| `prmptr.org` domain + DNS | live, resolving to Cloudflare; registrar account UNKNOWN | log into the Cloudflare account — it keeps running without this machine |
| Per-project Claude memory files | `~/.claude/projects/-home-camille-projects-prmptr-org/memory/` | already mirrored into the repo's `CLAUDE.md` |

## Has no copy anywhere
The roadmap design handoff above — 64 KB in one directory, outside git, source zip gone.
Everything else is duplicated: the tree is clean and equal to `origin/main` (`c3de847`),
all four `.claude/worktrees/worktree{1..4}` are clean with tips already contained in
`origin/main`, and `CLAUDE.md` / `AGENTS.md` — the project's accumulated knowledge — are
committed rather than stranded in session transcripts.

## Gotchas
- `.gitignore` lists `CLAUDE.md`, `AGENTS.md` and `.claude/`, but the two md files are
  force-tracked and current. A *new* file matching those patterns is silently skipped by
  `git add` — use `git add -f`.
- `CLAUDE.md` claims Tauri PNG icons are gitignored and must be copied into fresh
  worktrees. Stale: `.gitignore` re-includes `src-tauri/icons/**/*.png` and all icons are
  tracked. Skip that step.
- No code signing: `release.yml` has the signing env vars commented out, so Windows builds
  are unsigned and SmartScreen warns. Nothing to recover.
- `npm run test:e2e` needs `npx playwright install chromium`.
